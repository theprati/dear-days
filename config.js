// ── Dear Days configuration ──────────────────────────────
// Cloud backup is OPTIONAL. The app works fully on-device without it.
// When your Supabase project is created, fill these two values
// (Supabase dashboard → Project Settings → API):
window.DEAR_DAYS_CONFIG = {
  SUPABASE_URL: "",      // e.g. "https://abcdefgh.supabase.co"
  SUPABASE_ANON_KEY: ""  // the long "anon / public" key
};
